import React from "react";
import { View, Text, StyleSheet } from "react-native";

const ObjectiveMainScreen = () => {
  return <View></View>;
};

const styles = StyleSheet.create({});

export default ObjectiveMainScreen;
